from django.contrib.auth.models import AbstractUser
from django.db import models

#Creating a new CustomUser model which extends AbstractUser
class CustomUser(AbstractUser):
    pass
# Create your models here.
